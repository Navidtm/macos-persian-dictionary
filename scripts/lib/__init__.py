"""Shared library code for the English -> Persian Dictionary build tooling.

Kept deliberately dependency-free (standard library only) so that
contributors can run `make build` / `make test` without installing
anything beyond Python 3.
"""
